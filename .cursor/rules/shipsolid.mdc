---
description: ShipSolid non-negotiables — efficient code that does not break; orchestrate algorithm-reasoning then test-agent before done.
alwaysApply: true
---

# ShipSolid agents

This project is an **agent kit** for reliable code — not a learning wiki.

## Destination

Reliable code means:

1. **Efficient enough** for real constraints (complexity / scale).
2. **Doesn’t break** on scenarios for the current scope (unit → edges → integration as the system grows). Old tests stay.

Not the destination: cosmetics/readability-as-product, approving every micro-decision, or dumping the whole algorithm KB into context.

## Orchestration

For implement / optimize / “make it reliable” work, follow **`shipsolid`** (`.cursor/skills/shipsolid/SKILL.md`):

1. Intent → 2. **algorithm-reasoning** → 3. Code → 4. **test-agent** → 5. Suite green → DONE  

Do not call work done after code that only “looks good.”

## Roles

- **shipsolid** — orchestrator (full pipeline)
- **algorithm-reasoning** — efficiency / algorithm judgment (KB)
- **test-agent** — growing test suite; run; fail loud

## Knowledge base

Algorithm notes live in `BemnetMussa/algorithm-knowledge-base` (local clone if available). This repo references that map; it does not replace it.

## Autonomy

Command → happen. Ask only when intent or constraints are genuinely ambiguous.
